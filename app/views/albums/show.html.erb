<% content_for(:title) { get_local_value(I18n.locale, {en: @album.title, ua: @album.title_ua}) } %>

<p><%= get_local_value(I18n.locale, {en: @album.description, ua: @album.description_ua}) %></p>

<%= render 'images/gallery', images: @images %>
